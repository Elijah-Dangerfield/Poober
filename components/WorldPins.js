import React, { useState } from "react";
import styled from "styled-components";

const WorldPins = () => {
  return <Text>No one has dropped a pin here yet. Be the first!</Text>;
};

export default WorldPins;
const Text = styled.Text``;
