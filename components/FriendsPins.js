import React, { useState } from "react";
import styled from "styled-components";

const FriendsPins = () => {
  return <Text>Add friends to see where they drop their pins</Text>;
};

export default FriendsPins;
const Text = styled.Text``;
